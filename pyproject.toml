[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cvelab"
version = "0.1.0"
description = "Local-only CVE lab and safe PoC generator"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
cvelab = "cvelab.cli:main"

[tool.setuptools]
packages = ["cvelab"]
